Download and extract the parameterisation.tar folder from the ERDA repository here to use the notebook. That folder contains input pdb structures from EBI AF as well as simulation data and sigma(r) data.
